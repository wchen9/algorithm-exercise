package org.wangc.algo.slidingwindow;

/**
 * 06. 给定一个仅有小写字母的字符串，假定允许用任意字符替换不超过 k 个字母，找到最长的替换后仅包含相同字符的子串。
 */
public class CharacterReplacement {

    public static int findLength(String str, int k) {
        return -1;
    }

}
