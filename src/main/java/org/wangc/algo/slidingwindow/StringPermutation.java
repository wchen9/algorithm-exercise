package org.wangc.algo.slidingwindow;

/**
 * 08. 给定两个字符串 pattern 和 str，写一个函数来判断 str 是否包含 pattern 的排列。
 */
public class StringPermutation {
    public static boolean findPermutation(String str, String pattern) {
        return false;
    }
}
